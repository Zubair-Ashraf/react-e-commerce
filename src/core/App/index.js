import React from 'react';
import Routes from '../Routes';
import { Layout } from 'components';

const App = () => {
  return (
    <Layout>
      <Routes />
    </Layout>
  );
};

export default App;
